language: minimal

services:
    docker

jobs:
    include:
      - stage: backend-lint
        script: make backend.lint
      - stage: backend-test
        script: make run-tests 
        after_Script: make clean
